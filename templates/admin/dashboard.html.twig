{# templates/admin/my_dashboard.html.twig #}
{% extends '@EasyAdmin/layout.html.twig' %}

{% block main %}
    <div class="container mt-4">
        <h1>Admin Dashboard</h1>
        <div class="row">
            <div class="col-md-6">
                <h2>Users</h2>
                <a href="{{ userUrl }}" class="btn btn-primary">Manage Users</a>
            </div>
            <div class="col-md-6">
                <h2>Categories</h2>
                <a href="{{ categoryUrl }}" class="btn btn-primary">Manage Categories</a>
            </div>
        </div>
    </div>
{% endblock main %}